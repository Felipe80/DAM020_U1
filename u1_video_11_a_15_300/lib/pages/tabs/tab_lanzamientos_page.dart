import 'package:flutter/material.dart';

class TabsLanzamientosPage extends StatelessWidget {
  const TabsLanzamientosPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Lanzamientos',
        style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
      ),
    );
  }
}
